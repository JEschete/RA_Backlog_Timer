# RA_Backlog_Timer
Python tool that pulls your RetroAchievements "Want to Play" list via API and fetches HowLongToBeat completion times for each game. Features GUI login with secure credential storage (keyring), progress caching, fuzzy match detection, and Excel export with beat/completionist times and match quality notes.
